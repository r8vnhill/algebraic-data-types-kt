plugins {
    // Apply the custom Gradle plugin defined in 'jvm.conventions.gradle.kts'
    // This plugin standardizes settings such as Java toolchain version and applies the Kotlin JVM plugin.
    id("jvm.conventions")
}
