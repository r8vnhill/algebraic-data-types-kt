[versions]
# Define a version alias for Kotlin to keep version numbers centralized
kotlin = "2.2.0"

[libraries]
# Define a dependency alias for the Kotlin Gradle plugin
# This uses the version referenced above with `version.ref`
kotlin-gradle-plugin = { module = "org.jetbrains.kotlin:kotlin-gradle-plugin", version.ref = "kotlin" }
